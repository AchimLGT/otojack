-- Oyunun Player ve Character objelerine erişim için gerekli değişkenler
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local TextChatService = game:GetService("TextChatService")

-- GUI oluşturma
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = player:WaitForChild("PlayerGui")
ScreenGui.Name = "GrimmngGui"
ScreenGui.ResetOnSpawn = false -- Karakter yeniden doğduğunda GUI sıfırlanmasın

-- Oyunun Player ve Character objelerine erişim için gerekli değişkenler
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local TextChatService = game:GetService("TextChatService")

-- GUI oluşturma
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = player:WaitForChild("PlayerGui")
ScreenGui.Name = "GrimmngGui"
ScreenGui.ResetOnSpawn = false

-- Karakter yeniden doğduğunda GUI'nin kaybolmasını önle
player.CharacterAdded:Connect(function(newCharacter)
    character = newCharacter
    humanoid = newCharacter:WaitForChild("Humanoid")
    if not ScreenGui.Parent then
        ScreenGui.Parent = player:WaitForChild("PlayerGui")
    end
end)

-- Ana çerçeve (Frame) oluşturma
local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 600, 0, 400)
Frame.Position = UDim2.new(0.5, -300, 0.5, -200)
Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Frame.BorderSizePixel = 0
Frame.ClipsDescendants = true
local FrameCorner = Instance.new("UICorner")
FrameCorner.CornerRadius = UDim.new(0, 10)
FrameCorner.Parent = Frame
Frame.Parent = ScreenGui

-- Sürükleme için yardımcı değişkenler
local dragging = false
local dragInput, dragStart, startPos

Frame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = Frame.Position
    end
end)

Frame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

Frame.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
        dragInput = nil
    end
end)

game:GetService("RunService").RenderStepped:Connect(function()
    if dragging and dragInput then
        local delta = dragInput.Position - dragStart
        Frame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

-- Sağ Shift ile GUI açma/kapama
local UserInputService = game:GetService("UserInputService")
local guiVisible = true

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.RightShift then
        guiVisible = not guiVisible
        ScreenGui.Enabled = guiVisible
        if guiVisible then
            -- GUI açıldığında pozisyonu zorla güncelle
            Frame.Position = Frame.Position -- Mevcut pozisyonu koru
            Frame.BackgroundTransparency = 0
            Frame.Visible = true -- Görünürlüğü garantile
        end
    end
end)

-- Karakter yeniden doğduğunda GUI'nin kaybolmasını önle
player.CharacterAdded:Connect(function(newCharacter)
    -- Yeni karakteri bekle ve humanoid'i güncelle
    character = newCharacter
    humanoid = newCharacter:WaitForChild("Humanoid")
    -- GUI'nin PlayerGui içinde olduğundan emin ol
    if not ScreenGui.Parent then
        ScreenGui.Parent = player:WaitForChild("PlayerGui")
    end
end)

-- Ana çerçeve (Frame) oluşturma (buradan sonrası değişmeden devam eder)
local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 600, 0, 400) -- Yatay dikdörtgen
Frame.Position = UDim2.new(0.5, -300, 0.5, -200) -- Ortada
Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30) -- Koyu gri arka plan
Frame.BorderSizePixel = 0
local FrameCorner = Instance.new("UICorner")
FrameCorner.CornerRadius = UDim.new(0, 10)
FrameCorner.Parent = Frame
Frame.Parent = ScreenGui

-- Sürükleme için yardımcı değişkenler
local dragging = false
local dragInput, dragStart, startPos

Frame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = Frame.Position
    end
end)

Frame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

Frame.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
        dragInput = nil -- dragInput'u sıfırlıyoruz
    end
end)

game:GetService("RunService").RenderStepped:Connect(function()
    if dragging and dragInput then
        local delta = dragInput.Position - dragStart
        Frame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

-- Başlık (grimmng)
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(0, 600, 0, 40)
Title.Position = UDim2.new(0, 0, 0, 5)
Title.BackgroundColor3 = Color3.fromRGB(20, 20, 20) -- Daha koyu başlık çubuğu
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Text = "grimmng"
Title.TextSize = 24
Title.BorderSizePixel = 0
local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 10)
TitleCorner.Parent = Title
Title.Parent = Frame

-- Sol tarafta sekme çubuğu
local TabFrame = Instance.new("Frame")
TabFrame.Size = UDim2.new(0, 150, 0, 360)
TabFrame.Position = UDim2.new(0, 0, 0, 40)
TabFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40) -- Biraz daha açık gri
TabFrame.BorderSizePixel = 0
TabFrame.Parent = Frame

-- Sekme butonları
local tabs = {"Giriş", "Jackler", "Karakter", "Görüş"}
local tabButtons = {}
local currentTab = "Giriş"

for i, tabName in ipairs(tabs) do
    local TabButton = Instance.new("TextButton")
    TabButton.Size = UDim2.new(0, 150, 0, 40)
    TabButton.Position = UDim2.new(0, 0, 0, (i-1)*45)
    TabButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    TabButton.TextColor3 = Color3.fromRGB(200, 200, 200)
    TabButton.Text = tabName
    TabButton.TextSize = 18
    TabButton.BorderSizePixel = 0
    local TabButtonCorner = Instance.new("UICorner")
    TabButtonCorner.CornerRadius = UDim.new(0, 8)
    TabButtonCorner.Parent = TabButton
    TabButton.Parent = TabFrame
    tabButtons[tabName] = TabButton
end

-- Sağ tarafta içerik alanı
local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(0, 450, 0, 360)
ContentFrame.Position = UDim2.new(0, 150, 0, 40)
ContentFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ContentFrame.BackgroundTransparency = 0.4 -- Şeffaf, sadece düzen için
ContentFrame.BorderSizePixel = 0
ContentFrame.Parent = Frame

-- Jackler sekmesi için Jumper Jack GUI elemanları
local JacklerContent = Instance.new("Frame")
JacklerContent.Size = UDim2.new(1, 0, 1, 0)
JacklerContent.Position = UDim2.new(0, 0, 0, 0)
JacklerContent.BackgroundTransparency = 1
JacklerContent.Visible = false -- Varsayılan olarak gizli
JacklerContent.Parent = ContentFrame

-- Başlangıç TextBox
local StartTextBox = Instance.new("TextBox")
StartTextBox.Size = UDim2.new(0, 420, 0, 30) -- Genişlik 400 piksele çıkarıldı
StartTextBox.Position = UDim2.new(0, 20, 0, 40) -- x = 40, sağa hizalı olacak
StartTextBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
StartTextBox.TextColor3 = Color3.fromRGB(200, 200, 200)
StartTextBox.PlaceholderColor3 = Color3.fromRGB(150, 150, 150)
StartTextBox.PlaceholderText = "Başlangıç (örn: 5)"
StartTextBox.Text = ""
StartTextBox.BorderSizePixel = 0
local StartTextBoxCorner = Instance.new("UICorner")
StartTextBoxCorner.CornerRadius = UDim.new(0, 8)
StartTextBoxCorner.Parent = StartTextBox
StartTextBox.Parent = JacklerContent

-- Bitiş TextBox
local EndTextBox = Instance.new("TextBox")
EndTextBox.Size = UDim2.new(0, 420, 0, 30) -- Genişlik 400 piksele çıkarıldı
EndTextBox.Position = UDim2.new(0, 20, 0, 80) -- x = 40, sağa hizalı olacak
EndTextBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
EndTextBox.TextColor3 = Color3.fromRGB(200, 200, 200)
EndTextBox.PlaceholderColor3 = Color3.fromRGB(150, 150, 150)
EndTextBox.PlaceholderText = "Bitiş (örn: 9999)"
EndTextBox.Text = ""
EndTextBox.BorderSizePixel = 0
local EndTextBoxCorner = Instance.new("UICorner")
EndTextBoxCorner.CornerRadius = UDim.new(0, 8)
EndTextBoxCorner.Parent = EndTextBox
EndTextBox.Parent = JacklerContent

-- Zıpla butonu
local StartButton = Instance.new("TextButton")
StartButton.Size = UDim2.new(0, 200, 0, 40) -- Genişlik 200 piksele çıkarıldı (400/2)
StartButton.Position = UDim2.new(0, 20, 0, 130) -- x = 40
StartButton.BackgroundColor3 = Color3.fromRGB(70, 130, 180)
StartButton.TextColor3 = Color3.fromRGB(255, 255, 255)
StartButton.Text = "Zıpla!"
StartButton.BorderSizePixel = 0
local StartButtonCorner = Instance.new("UICorner")
StartButtonCorner.CornerRadius = UDim.new(0, 8)
StartButtonCorner.Parent = StartButton
StartButton.Parent = JacklerContent

-- Durdur/Devam Et butonu
local PauseButton = Instance.new("TextButton")
PauseButton.Size = UDim2.new(0, 200, 0, 40) -- Genişlik 200 piksele çıkarıldı (400/2)
PauseButton.Position = UDim2.new(0, 240, 0, 130) -- x = 40 + 200 (Zıpla butonunun hemen sağı)
PauseButton.BackgroundColor3 = Color3.fromRGB(200, 120, 50)
PauseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
PauseButton.Text = "Durdur"
PauseButton.BorderSizePixel = 0
local PauseButtonCorner = Instance.new("UICorner")
PauseButtonCorner.CornerRadius = UDim.new(0, 8)
PauseButtonCorner.Parent = PauseButton
PauseButton.Parent = JacklerContent

-- JJ/GJ/HJ Mod butonu
local ModeButton = Instance.new("TextButton")
ModeButton.Size = UDim2.new(0, 420, 0, 40) -- Genişlik 400 piksele çıkarıldı
ModeButton.Position = UDim2.new(0, 20, 0, 190) -- x = 40, sağa hizalı olacak
ModeButton.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
ModeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ModeButton.Text = "JJ"
ModeButton.BorderSizePixel = 0
local ModeButtonCorner = Instance.new("UICorner")
ModeButtonCorner.CornerRadius = UDim.new(0, 8)
ModeButtonCorner.Parent = ModeButton
ModeButton.Parent = JacklerContent

-- Turbo Modu butonu
local TurboButton = Instance.new("TextButton")
TurboButton.Size = UDim2.new(0, 420, 0, 40) -- Genişlik 400 piksele çıkarıldı
TurboButton.Position = UDim2.new(0, 20, 0, 240) -- x = 40, sağa hizalı olacak
TurboButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
TurboButton.TextColor3 = Color3.fromRGB(255, 255, 255)
TurboButton.Text = "Turbo Kapalı"
TurboButton.BorderSizePixel = 0
local TurboButtonCorner = Instance.new("UICorner")
TurboButtonCorner.CornerRadius = UDim.new(0, 8)
TurboButtonCorner.Parent = TurboButton
TurboButton.Parent = JacklerContent

-- Sekme içerik çerçeveleri
local tabContents = {}
for _, tabName in ipairs(tabs) do
    local content = Instance.new("Frame")
    content.Size = UDim2.new(1, 0, 1, 0)
    content.Position = UDim2.new(0, 0, 0, 0)
    content.BackgroundTransparency = 1
    content.Visible = false
    content.Parent = ContentFrame
    tabContents[tabName] = content
end

-- Jackler sekmesi içeriğini bağla
JacklerContent.Visible = false
tabContents["Jackler"] = JacklerContent
-- Karakter sekmesi için içerik
local CharacterContent = Instance.new("Frame")
CharacterContent.Size = UDim2.new(1, 0, 1, 0)
CharacterContent.Position = UDim2.new(0, 0, 0, 0)
CharacterContent.BackgroundTransparency = 1
CharacterContent.Visible = false
CharacterContent.Parent = ContentFrame

-- Walkspeed Yazısı
local WalkspeedLabel = Instance.new("TextLabel")
WalkspeedLabel.Size = UDim2.new(0, 200, 0, 30)
WalkspeedLabel.Position = UDim2.new(0, 40, 0, 40)
WalkspeedLabel.BackgroundTransparency = 1
WalkspeedLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
WalkspeedLabel.Text = "Walkspeed: 16"
WalkspeedLabel.TextSize = 18
WalkspeedLabel.Parent = CharacterContent

-- Walkspeed Slider
local WalkspeedSliderFrame = Instance.new("Frame")
WalkspeedSliderFrame.Size = UDim2.new(0, 360, 0, 10)
WalkspeedSliderFrame.Position = UDim2.new(0, 40, 0, 80)
WalkspeedSliderFrame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
WalkspeedSliderFrame.BorderSizePixel = 0
local WalkspeedSliderCorner = Instance.new("UICorner")
WalkspeedSliderCorner.CornerRadius = UDim.new(0, 5)
WalkspeedSliderCorner.Parent = WalkspeedSliderFrame
WalkspeedSliderFrame.Parent = CharacterContent

local WalkspeedSliderKnob = Instance.new("TextButton")
WalkspeedSliderKnob.Size = UDim2.new(0, 20, 0, 20)
WalkspeedSliderKnob.Position = UDim2.new(0, 0, -0.5, 0)
WalkspeedSliderKnob.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
WalkspeedSliderKnob.Text = ""
WalkspeedSliderKnob.BorderSizePixel = 0
local WalkspeedKnobCorner = Instance.new("UICorner")
WalkspeedKnobCorner.CornerRadius = UDim.new(0, 10)
WalkspeedKnobCorner.Parent = WalkspeedSliderKnob
WalkspeedSliderKnob.Parent = WalkspeedSliderFrame

-- Jumppower Yazısı
local JumppowerLabel = Instance.new("TextLabel")
JumppowerLabel.Size = UDim2.new(0, 200, 0, 30)
JumppowerLabel.Position = UDim2.new(0, 40, 0, 120)
JumppowerLabel.BackgroundTransparency = 1
JumppowerLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
JumppowerLabel.Text = "Jumppower: 20"
JumppowerLabel.TextSize = 18
JumppowerLabel.Parent = CharacterContent

-- Jumppower Slider
local JumppowerSliderFrame = Instance.new("Frame")
JumppowerSliderFrame.Size = UDim2.new(0, 360, 0, 10)
JumppowerSliderFrame.Position = UDim2.new(0, 40, 0, 160)
JumppowerSliderFrame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
JumppowerSliderFrame.BorderSizePixel = 0
local JumppowerSliderCorner = Instance.new("UICorner")
JumppowerSliderCorner.CornerRadius = UDim.new(0, 5)
JumppowerSliderCorner.Parent = JumppowerSliderFrame
JumppowerSliderFrame.Parent = CharacterContent

local JumppowerSliderKnob = Instance.new("TextButton")
JumppowerSliderKnob.Size = UDim2.new(0, 20, 0, 20)
JumppowerSliderKnob.Position = UDim2.new(0, 0, -0.5, 0)
JumppowerSliderKnob.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
JumppowerSliderKnob.Text = ""
JumppowerSliderKnob.BorderSizePixel = 0
local JumppowerKnobCorner = Instance.new("UICorner")
JumppowerKnobCorner.CornerRadius = UDim.new(0, 10)
JumppowerKnobCorner.Parent = JumppowerSliderKnob
JumppowerSliderKnob.Parent = JumppowerSliderFrame

-- Karakter sekmesini tabContents'e bağla
tabContents["Karakter"] = CharacterContent
-- Giriş sekmesi için içerik
local IntroContent = Instance.new("Frame")
IntroContent.Size = UDim2.new(1, 0, 1, 0)
IntroContent.Position = UDim2.new(0, 0, 0, 0)
IntroContent.BackgroundTransparency = 1
IntroContent.Visible = false
IntroContent.Parent = ContentFrame

-- Büyük başlık yazısı (Header)
local HeaderLabel = Instance.new("TextLabel")
HeaderLabel.Size = UDim2.new(0, 400, 0, 50)
HeaderLabel.Position = UDim2.new(0, 40, 0, 100)
HeaderLabel.BackgroundTransparency = 1
HeaderLabel.TextColor3 = Color3.fromRGB(255, 255, 255) -- Beyaz renk
HeaderLabel.Text = "Hoş Geldiniz!"
HeaderLabel.TextSize = 36 -- Büyük yazı boyutu
HeaderLabel.TextWrapped = true -- Metin taşarsa satır atlar
HeaderLabel.Parent = IntroContent

-- İlk küçük yazı
local SubText1 = Instance.new("TextLabel")
SubText1.Size = UDim2.new(0, 400, 0, 30)
SubText1.Position = UDim2.new(0, 40, 0, 160)
SubText1.BackgroundTransparency = 1
SubText1.TextColor3 = Color3.fromRGB(150, 150, 150) -- Gri ton
SubText1.Text = "grimmng private"
SubText1.TextSize = 18 -- Daha küçük yazı boyutu
SubText1.TextWrapped = true
SubText1.Parent = IntroContent

-- İkinci küçük yazı
local SubText2 = Instance.new("TextLabel")
SubText2.Size = UDim2.new(0, 400, 0, 30)
SubText2.Position = UDim2.new(0, 40, 0, 190)
SubText2.BackgroundTransparency = 1
SubText2.TextColor3 = Color3.fromRGB(150, 150, 150) -- Gri ton
SubText2.Text = "v1.0.0"
SubText2.TextSize = 18 -- Daha küçük yazı boyutu
SubText2.TextWrapped = true
SubText2.Parent = IntroContent

-- Giriş sekmesini tabContents'e bağla
tabContents["Giriş"] = IntroContent
-- Görüş sekmesi için içerik
local ViewContent = Instance.new("Frame")
ViewContent.Size = UDim2.new(1, 0, 1, 0)
ViewContent.Position = UDim2.new(0, 0, 0, 0)
ViewContent.BackgroundTransparency = 1
ViewContent.Visible = false
ViewContent.Parent = ContentFrame

-- ESP Toggle Butonu
local ESPToggleButton = Instance.new("TextButton")
ESPToggleButton.Size = UDim2.new(0, 350, 0, 40)
ESPToggleButton.Position = UDim2.new(0, 40, 0, 40)
ESPToggleButton.BackgroundColor3 = Color3.fromRGB(70, 130, 180)
ESPToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ESPToggleButton.Text = "ESP Aç"
ESPToggleButton.TextSize = 18
ESPToggleButton.BorderSizePixel = 0
local ESPToggleButtonCorner = Instance.new("UICorner")
ESPToggleButtonCorner.CornerRadius = UDim.new(0, 8)
ESPToggleButtonCorner.Parent = ESPToggleButton
ESPToggleButton.Parent = ViewContent

-- Takım Renklerini Kullan Butonu
local TeamColorToggleButton = Instance.new("TextButton")
TeamColorToggleButton.Size = UDim2.new(0, 350, 0, 40)
TeamColorToggleButton.Position = UDim2.new(0, 40, 0, 90)
TeamColorToggleButton.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
TeamColorToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
TeamColorToggleButton.Text = "Takım Renklerini Kullan"
TeamColorToggleButton.TextSize = 18
TeamColorToggleButton.BorderSizePixel = 0
local TeamColorToggleButtonCorner = Instance.new("UICorner")
TeamColorToggleButtonCorner.CornerRadius = UDim.new(0, 8)
TeamColorToggleButtonCorner.Parent = TeamColorToggleButton
TeamColorToggleButton.Parent = ViewContent

-- Renk Slider'ları için bir Frame (R, G, B için ayrı slider'lar)
local ColorSliderFrame = Instance.new("Frame")
ColorSliderFrame.Size = UDim2.new(0, 400, 0, 120)
ColorSliderFrame.Position = UDim2.new(0, 40, 0, 140) -- Takım renkleri butonunun altına kaydırıldı
ColorSliderFrame.BackgroundTransparency = 1
ColorSliderFrame.Parent = ViewContent

-- Kırmızı (R) Slider
local RLabel = Instance.new("TextLabel")
RLabel.Size = UDim2.new(0, 400, 0, 20)
RLabel.Position = UDim2.new(0, -20, 0, 0)
RLabel.BackgroundTransparency = 1
RLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
RLabel.Text = "Kırmızı: 255"
RLabel.TextSize = 16
RLabel.Parent = ColorSliderFrame

local RSliderFrame = Instance.new("Frame")
RSliderFrame.Size = UDim2.new(0, 350, 0, 10)
RSliderFrame.Position = UDim2.new(0, 0, 0, 20)
RSliderFrame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
RSliderFrame.BorderSizePixel = 0
local RSliderCorner = Instance.new("UICorner")
RSliderCorner.CornerRadius = UDim.new(0, 5)
RSliderCorner.Parent = RSliderFrame
RSliderFrame.Parent = ColorSliderFrame

local RSliderKnob = Instance.new("TextButton")
RSliderKnob.Size = UDim2.new(0, 20, 0, 20)
RSliderKnob.Position = UDim2.new(1, -20, -0.5, 0) -- Başlangıçta en sağda (255)
RSliderKnob.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
RSliderKnob.Text = ""
RSliderKnob.BorderSizePixel = 0
local RKnobCorner = Instance.new("UICorner")
RKnobCorner.CornerRadius = UDim.new(0, 10)
RKnobCorner.Parent = RSliderKnob
RSliderKnob.Parent = RSliderFrame

-- Yeşil (G) Slider
local GLabel = Instance.new("TextLabel")
GLabel.Size = UDim2.new(0, 400, 0, 20)
GLabel.Position = UDim2.new(0, -20, 0, 40)
GLabel.BackgroundTransparency = 1
GLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
GLabel.Text = "Yeşil: 255"
GLabel.TextSize = 16
GLabel.Parent = ColorSliderFrame

local GSliderFrame = Instance.new("Frame")
GSliderFrame.Size = UDim2.new(0, 350, 0, 10)
GSliderFrame.Position = UDim2.new(0, 0, 0, 60)
GSliderFrame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
GSliderFrame.BorderSizePixel = 0
local GSliderCorner = Instance.new("UICorner")
GSliderCorner.CornerRadius = UDim.new(0, 5)
GSliderCorner.Parent = GSliderFrame
GSliderFrame.Parent = ColorSliderFrame

local GSliderKnob = Instance.new("TextButton")
GSliderKnob.Size = UDim2.new(0, 20, 0, 20)
GSliderKnob.Position = UDim2.new(1, -20, -0.5, 0) -- Başlangıçta en sağda (255)
GSliderKnob.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
GSliderKnob.Text = ""
GSliderKnob.BorderSizePixel = 0
local GKnobCorner = Instance.new("UICorner")
GKnobCorner.CornerRadius = UDim.new(0, 10)
GKnobCorner.Parent = GSliderKnob
GSliderKnob.Parent = GSliderFrame

-- Mavi (B) Slider
local BLabel = Instance.new("TextLabel")
BLabel.Size = UDim2.new(0, 400, 0, 20)
BLabel.Position = UDim2.new(0, -20, 0, 80)
BLabel.BackgroundTransparency = 1
BLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
BLabel.Text = "Mavi: 255"
BLabel.TextSize = 16
BLabel.Parent = ColorSliderFrame

local BSliderFrame = Instance.new("Frame")
BSliderFrame.Size = UDim2.new(0, 350, 0, 10)
BSliderFrame.Position = UDim2.new(0, 0, 0, 100)
BSliderFrame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
BSliderFrame.BorderSizePixel = 0
local BSliderCorner = Instance.new("UICorner")
BSliderCorner.CornerRadius = UDim.new(0, 5)
BSliderCorner.Parent = BSliderFrame
BSliderFrame.Parent = ColorSliderFrame

local BSliderKnob = Instance.new("TextButton")
BSliderKnob.Size = UDim2.new(0, 20, 0, 20)
BSliderKnob.Position = UDim2.new(1, -20, -0.5, 0) -- Başlangıçta en sağda (255)
BSliderKnob.BackgroundColor3 = Color3.fromRGB(0, 0, 255)
BSliderKnob.Text = ""
BSliderKnob.BorderSizePixel = 0
local BKnobCorner = Instance.new("UICorner")
BKnobCorner.CornerRadius = UDim.new(0, 10)
BKnobCorner.Parent = BSliderKnob
BSliderKnob.Parent = BSliderFrame

-- Görüş sekmesini tabContents'e bağla
tabContents["Görüş"] = ViewContent

-- ESP Mantığı
local ESPEnabled = false
local ESPHighlights = {} -- Oyuncuların Highlight nesnelerini saklamak için tablo
local ESPNames = {} -- Oyuncuların isimlerini saklamak için tablo
local ESPColor = Color3.fromRGB(255, 255, 255) -- Varsayılan renk (beyaz)
local UseTeamColors = false -- Takım renklerini kullanma durumu

-- Slider mantığı (R, G, B için)
local function createColorSlider(sliderKnob, sliderFrame, label, colorComponent)
    local dragging = false
    local sliderWidth = sliderFrame.AbsoluteSize.X - sliderKnob.AbsoluteSize.X

    sliderKnob.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
        end
    end)

    sliderKnob.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)

    game:GetService("UserInputService").InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local mousePos = input.Position.X
            local framePos = sliderFrame.AbsolutePosition.X
            local relativePos = math.clamp((mousePos - framePos), 0, sliderWidth)
            local ratio = relativePos / sliderWidth
            sliderKnob.Position = UDim2.new(ratio, 0, -0.5, 0)

            -- Değeri hesapla (0-255 arası)
            local value = math.floor(ratio * 255 + 0.5)
            label.Text = colorComponent .. ": " .. value

            -- ESP rengini güncelle
            if colorComponent == "Kırmızı" then
                ESPColor = Color3.fromRGB(value, ESPColor.G * 255, ESPColor.B * 255)
            elseif colorComponent == "Yeşil" then
                ESPColor = Color3.fromRGB(ESPColor.R * 255, value, ESPColor.B * 255)
            elseif colorComponent == "Mavi" then
                ESPColor = Color3.fromRGB(ESPColor.R * 255, ESPColor.G * 255, value)
            end

            -- ESP açıkken ve takım renkleri kullanılmıyorken renkleri güncelle
            if ESPEnabled and not UseTeamColors then
                for _, highlight in pairs(ESPHighlights) do
                    highlight.FillColor = ESPColor
                    highlight.OutlineColor = ESPColor
                end
            end
        end
    end)
end

-- Slider'ları başlat
createColorSlider(RSliderKnob, RSliderFrame, RLabel, "Kırmızı")
createColorSlider(GSliderKnob, GSliderFrame, GLabel, "Yeşil")
createColorSlider(BSliderKnob, BSliderFrame, BLabel, "Mavi")

-- ESP renklerini güncelleyen yardımcı fonksiyon
local function updateESPColors()
    if ESPEnabled then
        for otherPlayer, highlight in pairs(ESPHighlights) do
            if UseTeamColors and otherPlayer.Team then
                highlight.FillColor = otherPlayer.TeamColor.Color
                highlight.OutlineColor = otherPlayer.TeamColor.Color
            else
                highlight.FillColor = ESPColor
                highlight.OutlineColor = ESPColor
            end
        end
    end
end

-- Takım renklerini açıp kapatma fonksiyonu
local function toggleTeamColors()
    UseTeamColors = not UseTeamColors
    TeamColorToggleButton.Text = UseTeamColors and "Takım Renklerini Kapat" or "Takım Renklerini Kullan"
    updateESPColors()
end

-- ESP'yi açıp kapatma fonksiyonu
local function toggleESP()
    ESPEnabled = not ESPEnabled
    ESPToggleButton.Text = ESPEnabled and "ESP Kapat" or "ESP Aç"

    if ESPEnabled then
        -- Tüm oyuncular için ESP'yi etkinleştir
        for _, otherPlayer in pairs(game.Players:GetPlayers()) do
            if otherPlayer ~= player and otherPlayer.Character then
                local character = otherPlayer.Character
                local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
                if humanoidRootPart then
                    -- Highlight oluştur
                    local highlight = Instance.new("Highlight")
                    highlight.Adornee = character
                    if UseTeamColors and otherPlayer.Team then
                        highlight.FillColor = otherPlayer.TeamColor.Color
                        highlight.OutlineColor = otherPlayer.TeamColor.Color
                    else
                        highlight.FillColor = ESPColor
                        highlight.OutlineColor = ESPColor
                    end
                    highlight.FillTransparency = 0.5
                    highlight.OutlineTransparency = 0
                    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop -- Duvarların arkasından görünür
                    highlight.Parent = character
                    ESPHighlights[otherPlayer] = highlight

                    -- İsim için BillboardGui oluştur
                    local billboardGui = Instance.new("BillboardGui")
                    billboardGui.Size = UDim2.new(0, 100, 0, 50)
                    billboardGui.StudsOffset = Vector3.new(0, 3, 0) -- Başın üstünde
                    billboardGui.AlwaysOnTop = true -- Duvarların arkasından görünür
                    billboardGui.Adornee = humanoidRootPart
                    billboardGui.Parent = character

                    local nameLabel = Instance.new("TextLabel")
                    nameLabel.Size = UDim2.new(1, 0, 1, 0)
                    nameLabel.BackgroundTransparency = 1
                    nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
                    nameLabel.TextStrokeTransparency = 0
                    nameLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                    nameLabel.TextScaled = true
                    nameLabel.Text = otherPlayer.Name
                    nameLabel.Parent = billboardGui
                    ESPNames[otherPlayer] = billboardGui
                end
            end
        end
    else
        -- ESP'yi kapat, tüm Highlight ve BillboardGui'leri kaldır
        for player, highlight in pairs(ESPHighlights) do
            highlight:Destroy()
        end
        for player, billboardGui in pairs(ESPNames) do
            billboardGui:Destroy()
        end
        ESPHighlights = {}
        ESPNames = {}
    end
end

-- Toggle butonlarına tıklama olayları
ESPToggleButton.MouseButton1Click:Connect(toggleESP)
TeamColorToggleButton.MouseButton1Click:Connect(toggleTeamColors)

-- Yeni oyuncular için ESP'yi güncelle
game.Players.PlayerAdded:Connect(function(newPlayer)
    if ESPEnabled and newPlayer ~= player then
        newPlayer.CharacterAdded:Connect(function(character)
            local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
            if humanoidRootPart then
                -- Highlight oluştur
                local highlight = Instance.new("Highlight")
                highlight.Adornee = character
                if UseTeamColors and newPlayer.Team then
                    highlight.FillColor = newPlayer.TeamColor.Color
                    highlight.OutlineColor = newPlayer.TeamColor.Color
                else
                    highlight.FillColor = ESPColor
                    highlight.OutlineColor = ESPColor
                end
                highlight.FillTransparency = 0.5
                highlight.OutlineTransparency = 0
                highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                highlight.Parent = character
                ESPHighlights[newPlayer] = highlight

                -- İsim için BillboardGui oluştur
                local billboardGui = Instance.new("BillboardGui")
                billboardGui.Size = UDim2.new(0, 100, 0, 50)
                billboardGui.StudsOffset = Vector3.new(0, 3, 0)
                billboardGui.AlwaysOnTop = true
                billboardGui.Adornee = humanoidRootPart
                billboardGui.Parent = character

                local nameLabel = Instance.new("TextLabel")
                nameLabel.Size = UDim2.new(1, 0, 1, 0)
                nameLabel.BackgroundTransparency = 1
                nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
                nameLabel.TextStrokeTransparency = 0
                nameLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                nameLabel.TextScaled = true
                nameLabel.Text = newPlayer.Name
                nameLabel.Parent = billboardGui
                ESPNames[newPlayer] = billboardGui
            end
        end)
    end
end)

-- Oyuncu ayrıldığında ESP'yi temizle
game.Players.PlayerRemoving:Connect(function(leavingPlayer)
    if ESPHighlights[leavingPlayer] then
        ESPHighlights[leavingPlayer]:Destroy()
        ESPHighlights[leavingPlayer] = nil
    end
    if ESPNames[leavingPlayer] then
        ESPNames[leavingPlayer]:Destroy()
        ESPNames[leavingPlayer] = nil
    end
end)

-- Oyuncunun takımı değiştiğinde ESP rengini güncelle
game.Players.PlayerAdded:Connect(function(newPlayer)
    newPlayer:GetPropertyChangedSignal("Team"):Connect(function()
        if ESPEnabled and UseTeamColors and ESPHighlights[newPlayer] then
            local highlight = ESPHighlights[newPlayer]
            if newPlayer.Team then
                highlight.FillColor = newPlayer.TeamColor.Color
                highlight.OutlineColor = newPlayer.TeamColor.Color
            else
                highlight.FillColor = ESPColor
                highlight.OutlineColor = ESPColor
            end
        end
    end)
end)

-- Slider sürükleme mantığı
local function createSlider(sliderKnob, sliderFrame, minValue, maxValue, label, property)
    local dragging = false
    local sliderWidth = sliderFrame.AbsoluteSize.X - sliderKnob.AbsoluteSize.X

    sliderKnob.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
        end
    end)

    sliderKnob.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)

    game:GetService("UserInputService").InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local mousePos = input.Position.X
            local framePos = sliderFrame.AbsolutePosition.X
            local relativePos = math.clamp((mousePos - framePos), 0, sliderWidth)
            local ratio = relativePos / sliderWidth
            sliderKnob.Position = UDim2.new(ratio, 0, -0.5, 0)

            -- Değeri hesapla
            local value = minValue + (maxValue - minValue) * ratio
            value = math.floor(value + 0.5) -- Yuvarla
            label.Text = property .. ": " .. value

            -- Karakterin özelliğini güncelle
            if humanoid then
                if property == "Walkspeed" then
                    humanoid.WalkSpeed = value
                elseif property == "Jumppower" then
                    humanoid.JumpPower = value
                end
            end
        end
    end)
end

-- Slider'ları başlat
createSlider(WalkspeedSliderKnob, WalkspeedSliderFrame, 16, 50, WalkspeedLabel, "Walkspeed")
createSlider(JumppowerSliderKnob, JumppowerSliderFrame, 20, 100, JumppowerLabel, "Jumppower")

-- Sekme değiştirme mantığı
local function switchTab(tabName)
    for name, button in pairs(tabButtons) do
        if name == tabName then
            button.BackgroundColor3 = Color3.fromRGB(70, 70, 70) -- Seçili sekme koyu gri
        else
            button.BackgroundColor3 = Color3.fromRGB(50, 50, 50) -- Diğer sekmeler açık gri
        end
    end
    for name, content in pairs(tabContents) do
        content.Visible = (name == tabName)
    end
    currentTab = tabName
end

-- Sekme butonlarına tıklama olayları
for tabName, button in pairs(tabButtons) do
    button.MouseButton1Click:Connect(function()
        switchTab(tabName)
    end)
end

-- Varsayılan olarak Giriş sekmesini aç
switchTab("Giriş")

-- Türkçe karakterler için büyükten küçüğe dönüşüm tablosu
local turkishLowerMap = {
    ["Ç"] = "ç", ["Ğ"] = "ğ", ["İ"] = "i", ["Ö"] = "ö", ["Ş"] = "ş", ["Ü"] = "ü",
    ["A"] = "a", ["B"] = "b", ["C"] = "c", ["D"] = "d", ["E"] = "e", ["F"] = "f",
    ["G"] = "g", ["H"] = "h", ["I"] = "ı", ["J"] = "j", ["K"] = "k", ["L"] = "l", -- Virgül eklendi
    ["M"] = "m", ["N"] = "n", ["O"] = "o", ["P"] = "p", ["R"] = "r", ["S"] = "s",
    ["T"] = "t", ["U"] = "u", ["V"] = "v", ["Y"] = "y", ["Z"] = "z"
}

local turkishUpperMap = {
    ["ç"] = "Ç", ["ğ"] = "Ğ", ["i"] = "İ", ["ö"] = "Ö", ["ş"] = "Ş", ["ü"] = "Ü",
    ["a"] = "A", ["b"] = "B", ["c"] = "C", ["d"] = "D", ["e"] = "E", ["f"] = "F",
    ["g"] = "G", ["h"] = "H", ["ı"] = "I", ["j"] = "J", ["k"] = "K", ["l"] = "L",
    ["m"] = "M", ["n"] = "N", ["o"] = "O", ["p"] = "P", ["r"] = "R", ["s"] = "S",
    ["t"] = "T", ["u"] = "U", ["v"] = "V", ["y"] = "Y", ["z"] = "Z"
}

local function customLower(str)
    local result = ""
    for _, codepoint in utf8.codes(str) do
        local char = utf8.char(codepoint)
        result = result .. (turkishLowerMap[char] or char)
    end
    return result
end

local function capitalizeFirst(str)
    if utf8.len(str) > 0 then
        local firstChar = ""
        for _, codepoint in utf8.codes(str) do
            firstChar = utf8.char(codepoint)
            break
        end
        local rest = str:sub(utf8.offset(str, 2) or #str + 1)
        return (turkishUpperMap[customLower(firstChar)] or firstChar:upper()) .. customLower(rest)
    end
    return str
end

-- Sayıları Türkçe olarak yazıya çevirme fonksiyonu (99999’a kadar destek)
local function numberToText(num, mode)
    local birler = {"", "BİR", "İKİ", "ÜÇ", "DÖRT", "BEŞ", "ALTI", "YEDİ", "SEKİZ", "DOKUZ"}
    local onlar = {"", "ON", "YİRMİ", "OTUZ", "KIRK", "ELLİ", "ALTMIŞ", "YETMİŞ", "SEKSEN", "DOKSAN"}
    local yuzler = {"", "YÜZ", "İKİ YÜZ", "ÜÇ YÜZ", "DÖRT YÜZ", "BEŞ YÜZ", "ALTI YÜZ", "YEDİ YÜZ", "SEKİZ YÜZ", "DOKUZ YÜZ"}
    
    local function formatGJ(str)
        if str == "" then return "" end
        local lowered = customLower(str)
        return capitalizeFirst(lowered) .. "."
    end
    
    local function baseText(num)
        if num == 0 then return "SIFIR" end
        if num <= 9 then return birler[num + 1] end
        if num <= 99 then
            local on = math.floor(num / 10)
            local bir = num % 10
            if bir == 0 then
                return onlar[on + 1]
            else
                return onlar[on + 1] .. " " .. birler[bir + 1]
            end
        end
        if num <= 999 then
            local yuz = math.floor(num / 100)
            local kalan = num % 100
            if kalan == 0 then
                return yuzler[yuz + 1]
            elseif kalan <= 9 then
                return yuzler[yuz + 1] .. " " .. birler[kalan + 1]
            else
                local on = math.floor(kalan / 10)
                local bir = kalan % 10
                if bir == 0 then
                    return yuzler[yuz + 1] .. " " .. onlar[on + 1]
                else
                    return yuzler[yuz + 1] .. " " .. onlar[on + 1] .. " " .. birler[bir + 1]
                end
            end
        end
        if num <= 9999 then
            local bin = math.floor(num / 1000)
            local kalan = num % 1000
            local binText = (bin == 1 and "BİN") or (birler[bin + 1] .. " BİN")
            if kalan == 0 then
                return binText
            elseif kalan <= 9 then
                return binText .. " " .. birler[kalan + 1]
            elseif kalan <= 99 then
                local on = math.floor(kalan / 10)
                local bir = kalan % 10
                if bir == 0 then
                    return binText .. " " .. onlar[on + 1]
                else
                    return binText .. " " .. onlar[on + 1] .. " " .. birler[bir + 1]
                end
            else
                local yuz = math.floor(kalan / 100)
                local kalan2 = kalan % 100
                if kalan2 == 0 then
                    return binText .. " " .. yuzler[yuz + 1]
                elseif kalan2 <= 9 then
                    return binText .. " " .. yuzler[yuz + 1] .. " " .. birler[kalan2 + 1]
                else
                    local on = math.floor(kalan2 / 10)
                    local bir = kalan2 % 10
                    if bir == 0 then
                        return binText .. " " .. yuzler[yuz + 1] .. " " .. onlar[on + 1]
                    else
                        return binText .. " " .. yuzler[yuz + 1] .. " " .. onlar[on + 1] .. " " .. birler[bir + 1]
                    end
                end
            end
        end
        if num <= 99999 then
            local onbin = math.floor(num / 10000)
            local kalan = num % 10000
            if onbin > 0 then
                local bin = math.floor(kalan / 1000)
                local kalan2 = kalan % 1000
                local onbinText
                if bin > 0 then
                    -- On binler ve binler birleşik şekilde işlenir
                    local onbinBinNum = onbin * 10 + bin
                    onbinText = onlar[onbinBinNum + 1] .. " BİN"
                else
                    onbinText = onlar[onbin + 1] .. " BİN"
                end
                if kalan2 == 0 then
                    return onbinText
                elseif kalan2 <= 9 then
                    return onbinText .. " " .. birler[kalan2 + 1]
                elseif kalan2 <= 99 then
                    local on = math.floor(kalan2 / 10)
                    local bir = kalan2 % 10
                    if bir == 0 then
                        return onbinText .. " " .. onlar[on + 1]
                    else
                        return onbinText .. " " .. onlar[on + 1] .. " " .. birler[bir + 1]
                    end
                else
                    local yuz = math.floor(kalan2 / 100)
                    local kalan3 = kalan2 % 100
                    if kalan3 == 0 then
                        return onbinText .. " " .. yuzler[yuz + 1]
                    elseif kalan3 <= 9 then
                        return onbinText .. " " .. yuzler[yuz + 1] .. " " .. birler[kalan3 + 1]
                    else
                        local on = math.floor(kalan3 / 10)
                        local bir = kalan3 % 10
                        if bir == 0 then
                            return onbinText .. " " .. yuzler[yuz + 1] .. " " .. onlar[on + 1]
                        else
                            return onbinText .. " " .. yuzler[yuz + 1] .. " " .. onlar[on + 1] .. " " .. birler[bir + 1]
                        end
                    end
                end
            else
                -- 10000'den küçük sayılar için eski mantık
                local bin = math.floor(num / 1000)
                local kalan = num % 1000
                local binText = (bin == 1 and "BİN") or (birler[bin + 1] .. " BİN")
                if kalan == 0 then
                    return binText
                elseif kalan <= 9 then
                    return binText .. " " .. birler[kalan + 1]
                elseif kalan <= 99 then
                    local on = math.floor(kalan / 10)
                    local bir = kalan % 10
                    if bir == 0 then
                        return binText .. " " .. onlar[on + 1]
                    else
                        return binText .. " " .. onlar[on + 1] .. " " .. birler[bir + 1]
                    end
                else
                    local yuz = math.floor(kalan / 100)
                    local kalan2 = kalan % 100
                    if kalan2 == 0 then
                        return binText .. " " .. yuzler[yuz + 1]
                    elseif kalan2 <= 9 then
                        return binText .. " " .. yuzler[yuz + 1] .. " " .. birler[kalan2 + 1]
                    else
                        local on = math.floor(kalan2 / 10)
                        local bir = kalan2 % 10
                        if bir == 0 then
                            return binText .. " " .. yuzler[yuz + 1] .. " " .. onlar[on + 1]
                        else
                            return binText .. " " .. yuzler[yuz + 1] .. " " .. onlar[on + 1] .. " " .. birler[bir + 1]
                        end
                    end
                end
            end
        end
    end
    
    local text = baseText(num)
    if mode == "JJ" then
        return text
    elseif mode == "GJ" then
        return formatGJ(text)
    elseif mode == "HJ" then
        return text
    end
end

-- Turbo durumu kontrol değişkeni
local isTurbo = false

-- Turbo Modu mantığı
TurboButton.MouseButton1Click:Connect(function()
    if isTurbo then
        isTurbo = false
        TurboButton.Text = "Turbo Kapalı"
        TurboButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
    else
        isTurbo = true
        TurboButton.Text = "Turbo Açık"
        TurboButton.BackgroundColor3 = Color3.fromRGB(70, 130, 180)
    end
end)

-- Mesaj gönderme fonksiyonu (Turbo 0.75 katına indiriyor, HJ’de etkisiz)
local function sendChatMessage(message)
    local charCount = utf8.len(message)
    local waitTime
    if charCount == 1 then
        waitTime = 0.7 -- 1 harf için 0.7 saniye
    else
        waitTime = 1.3 + (charCount * 0.05) -- Birden fazla harf için 1.3 + harf başına 0.05 saniye
    end
    if isTurbo and currentMode ~= "HJ" then
        waitTime = waitTime * 0.75
    end
    wait(waitTime)
    
    if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
        local textChannels = TextChatService:WaitForChild("TextChannels", 5)
        if textChannels then
            local textChannel = textChannels:WaitForChild("RBXGeneral", 5)
            if textChannel then
                textChannel:SendAsync(message)
                return
            end
        end
    end
    
    local chatEvent = game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents")
    if chatEvent and chatEvent:FindFirstChild("SayMessageRequest") then
        chatEvent.SayMessageRequest:FireServer(message, "All")
        return
    end
    
    print("Chat sistemi kullanılamıyor, mesaj yerel olarak yazdırıldı: " .. message)
end

-- Durum kontrol değişkenleri
local isPaused = false
local isRunning = false
local currentIndex = 1
local startNumber = 1
local targetNumber = 0
local currentMode = "JJ"

-- JJ/GJ/HJ Mod değiştirme mantığı
ModeButton.MouseButton1Click:Connect(function()
    if currentMode == "JJ" then
        currentMode = "GJ"
        ModeButton.Text = "GJ"
        ModeButton.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
    elseif currentMode == "GJ" then
        currentMode = "HJ"
        ModeButton.Text = "HJ"
        ModeButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    else
        currentMode = "JJ"
        ModeButton.Text = "JJ"
        ModeButton.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
    end
end)

-- Durdur/Devam Et butonu mantığı
PauseButton.MouseButton1Click:Connect(function()
    if isRunning then
        if isPaused then
            isPaused = false
            PauseButton.Text = "Durdur"
            while currentIndex <= targetNumber do
                if isPaused then break end
                local message = numberToText(currentIndex, currentMode)
                if currentMode == "HJ" then
                    for _, codepoint in utf8.codes(message) do
                        if isPaused then break end
                        local char = utf8.char(codepoint)
                        if char ~= " " then
                            sendChatMessage(char)
                            if humanoid then humanoid.Jump = true end
                        end
                    end
                end
                if not isPaused then
                    sendChatMessage(message)
                    if humanoid then humanoid.Jump = true end
                end
                currentIndex = currentIndex + 1
            end
            if not isPaused then
                isRunning = false
                currentIndex = 1
                PauseButton.Text = "Durdur"
            end
        else
            isPaused = true
            PauseButton.Text = "Devam Et"
        end
    end
end)

-- Zıpla butonu mantığı
StartButton.MouseButton1Click:Connect(function()
    local endInput = tonumber(EndTextBox.Text) or 99999
    local startInput = tonumber(StartTextBox.Text) or 1
    
    if endInput > 0 then
        if startInput > endInput then
            EndTextBox.Text = "Başlangıç bitişten büyük olamaz!"
            wait(1)
            EndTextBox.Text = ""
            return
        end
        if endInput > 99999 then
            EndTextBox.Text = "Maksimum 99999 olabilir!"
            wait(1)
            EndTextBox.Text = ""
            return
        end
        
        isRunning = true
        isPaused = false
        startNumber = startInput
        targetNumber = endInput
        currentIndex = startNumber
        PauseButton.Text = "Durdur"
        
        while currentIndex <= targetNumber do
            if isPaused then break end
            local message = numberToText(currentIndex, currentMode)
            if currentMode == "HJ" then
                for _, codepoint in utf8.codes(message) do
                    if isPaused then break end
                    local char = utf8.char(codepoint)
                    if char ~= " " then
                        sendChatMessage(char)
                        if humanoid then humanoid.Jump = true end
                    end
                end
            end
            if not isPaused then
                sendChatMessage(message)
                if humanoid then humanoid.Jump = true end
            end
            currentIndex = currentIndex + 1
        end
        if not isPaused then
            isRunning = false
            currentIndex = 1
        end
    else
        EndTextBox.Text = "Lütfen geçerli bir bitiş sayısı gir!"
        wait(1)
        EndTextBox.Text = ""
    end
end)
